apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-frontend-pvc
spec:
  storageClassName: todo-frontend-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
